import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: '멘토링 수정',
}

export default function MentoringEditLayout({ children }: { children: React.ReactNode }) {
  return children
}

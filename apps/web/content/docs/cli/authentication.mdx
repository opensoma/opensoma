---
title: Authentication
description: 인증 설정 및 관리
order: 1
---

모든 CLI 명령어는 인증이 필요합니다. 아이디와 비밀번호로 로그인합니다.

인증 정보는 `~/.config/opensoma/credentials.json`에 저장되며, 파일 권한은 `0600`으로 설정됩니다.

저장 위치를 변경하려면 `OPENSOMA_CONFIG_DIR` 환경변수를 설정하세요. 여러 계정을 분리하거나 컨테이너/CI 환경에서 격리된 경로를 사용할 때 유용합니다.

```bash
export OPENSOMA_CONFIG_DIR=/tmp/opensoma-ci
opensoma auth status
```

## Username / Password Login

아이디와 비밀번호로 직접 로그인합니다. 환경변수를 사용하면 스크립트에서 자격증명을 하드코딩하지 않아도 됩니다.

```bash
opensoma auth login --username <아이디> --password <비밀번호>
```

환경변수로도 전달 가능합니다:

```bash
export OPENSOMA_USERNAME=<아이디>
export OPENSOMA_PASSWORD=<비밀번호>
opensoma auth login
```

## Check Auth Status

현재 저장된 인증 정보의 유효성을 확인합니다. 세션이 만료되었거나 인증 정보가 없으면 오류를 반환합니다.

```bash
opensoma auth status
```

## Logout

```bash
opensoma auth logout
```

가능하면 SWMaestro 서버 세션 로그아웃도 함께 시도한 뒤, 로컬 인증 정보를 삭제합니다.
